import { Layout } from '@components/Layout';

const Logout: React.FC = () => {
  return (
    <Layout>
      <div className="container mx-auto p-4">
        <h1 className="mb-4 text-3xl font-bold">Logout Page</h1>
        {/* Logout logic goes here */}
      </div>
    </Layout>
  );
};

export default Logout;
